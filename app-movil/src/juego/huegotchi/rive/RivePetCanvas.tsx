/**
 * Rive desactivado en runtime (rompía el bundle web).
 * El contrato y brief siguen en RIVE_BRIEF.md / rive.contract.json
 * para cuando se reactive con lazy import + .riv real.
 */
export function RivePetCanvas(_props: Record<string, unknown>) {
  return null;
}
