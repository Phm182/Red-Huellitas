import React from 'react';
import { View } from 'react-native';
import { RivePetRuntimeProps } from './RivePetRuntime.types';

/**
 * Stub tipado. Metro sustituye por `.native.tsx` / `.web.tsx` en runtime.
 */
export function RivePetRuntime(_props: RivePetRuntimeProps) {
  return <View />;
}
