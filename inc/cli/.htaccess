# Defensa en profundidad: estos scripts se ejecutan por PHP CLI (Programador
# de Tareas de Windows), nunca deberían ser accedidos por HTTP.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
