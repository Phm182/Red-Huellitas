# Archivos privados (DNI, selfie, carnets). Sólo se sirven vía PHP autenticado.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
